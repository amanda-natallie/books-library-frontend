import { makeStyles, createStyles } from '@mui/styles'

export const useStyles = makeStyles(() =>
  createStyles({
    root: {
      right: 12,
      bottom: 0,
      position: 'fixed',
      top: -700
    }
  })
)
